[pytest]
# No `testpaths` here on purpose. macOS filesystems are case-insensitive by
# default, so the suite directory may appear as tests/ or Tests/ depending on
# which spelling created it first, and pinning one of them makes the config
# wrong on the other machine. pytest discovers test_*.py from the rootdir
# either way.
addopts = -q
